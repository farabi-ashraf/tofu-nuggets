{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for app windows",
  "windows": ["overlay", "badges", "editor", "main", "settings"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
