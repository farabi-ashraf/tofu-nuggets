{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for app windows",
  "windows": ["overlay", "main"],
  "permissions": ["core:default"]
}
