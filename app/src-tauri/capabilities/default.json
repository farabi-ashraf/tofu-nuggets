{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for app windows",
  "windows": ["overlay", "editor", "main", "settings"],
  "permissions": ["core:default", "dialog:allow-open"]
}
